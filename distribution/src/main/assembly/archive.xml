<?xml version="1.0" encoding="UTF-8"?>
<assembly>

    <id>distribution</id>

    <formats>
        <format>dir</format>
        <format>tar.gz</format>
        <format>zip</format>
    </formats>

    <fileSets>
        <!--
         | copy benchmarks scripts
        -->
        <fileSet>
            <directory>${project.build.directory}/orientdb-community-gremlin-${project.version}</directory>
            <outputDirectory></outputDirectory>
        </fileSet>
        <fileSet>
            <directory>${project.build.directory}/orientdb-teleporter</directory>
            <outputDirectory></outputDirectory>
        </fileSet>
    </fileSets>
    <!--
     | all artifacts in a /lib dir
    -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>lib</outputDirectory>
            <includes>
                <include>*:jar:*</include>
            </includes>
        </dependencySet>

    </dependencySets>

</assembly>
